# Priyanka Portfolio

Editorial React + Vite portfolio for Priyanka Vishwakarma.

This build keeps the stronger React/Tailwind foundation from the supplied portfolio archive and uses the real image/document assets already published in `Oflakstudio/Priyanka-vishwakarma-portfolio`.

The image and document references intentionally point to that public asset repository so the new repo does not duplicate binary files.
